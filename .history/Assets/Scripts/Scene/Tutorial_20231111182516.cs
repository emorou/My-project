using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Tutorial : MonoBehaviour
{
    void Update()
    {
        if(Input.GetKey(KeyCode.W) && Input.GetKey(KeyCode.A) Input.GetKey(KeyCode.W))
    }
}
